import styled from 'styled-components';

const MainWrapper = styled.section`
  right: 0;
  bottom: 0;
  left: 0;
  overflow-y: scroll;
  text-align: center;

  div {
    color: red
  }
  
`;

export default MainWrapper;