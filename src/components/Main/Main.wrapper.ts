import styled from 'styled-components';

const MainWrapper = styled.section`
  display: flex;
  right: 0;
  bottom: 0;
  left: 0;
  overflow-y: scroll;
  text-align: center;
  
`;

export default MainWrapper;