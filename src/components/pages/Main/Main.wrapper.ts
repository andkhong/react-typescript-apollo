import styled from 'styled-components';

const MainWrapper = styled.div`
  right: 0;
  left: 0;
  text-align: center;

  .title-container {
    height: 600px;
  }

  .listing-container {
  }

`;

export default MainWrapper;